/**
 * Optional slug translation map for future localized route names.
 * P0 uses identical slugs per locale under locale-prefixed paths.
 */
export const routeSlugMap: Record<string, Partial<Record<string, string>>> = {};
